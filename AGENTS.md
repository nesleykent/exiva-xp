# AGENTS.md — the Exiva XP architectural contract

This file is binding. Any agent (Codex, Claude, or other) continuing this project reads this file FIRST and follows it exactly. When an instruction here conflicts with what seems convenient, this file wins. Only the project owner (nesleykent) or the controlling architect may amend it — and every amendment is committed with a reason.

## 1. What this project is

**Exiva XP is a personal Tibia character intelligence hub for Night'Flyn** (Elder Druid, Gentebra) — private-first, with optional public features later. It centralises: Hunting Analyser sessions, XP and highscore history, profit, deaths, stamina planning, hunting places, creature data, elemental weaknesses, charms, and long-term progression. Every tool must answer a practical question about the character: *where should I hunt, which element is best, how much profit do I make, how does my XP evolve, which respawns fit my level, which creatures do I kill most, which charms matter, how does my performance change.*

The community hunting database is the planning engine underneath, not the product. The character is the centre.

**The only reference projects** (never add others without owner approval):
1. Exiva XP itself — https://nesleykent.github.io/exiva-xp/index.html
2. TibiaTools — https://tibiatools.io (damage/combat calculator logic: resistances, mitigation, crit, fatal, charms)
3. Respawn Finder — https://github.com/danyelvarejao/respawn-finder (respawn search patterns used to improve the hunting planner/database; do not add item timers, party split or blessing tools unless the owner explicitly asks)
4. Tibia XP History — https://github.com/mathiasbynens/tibia-xp-history (daily progression tracking; its formulas, crawl mechanism and 03:00 UTC schedule are ported, while row dates follow Tibia's 10:00 Europe/Berlin server-save day)

## 2. Architecture (do not deviate)

- **Plain HTML5 + CSS + vanilla JS ES modules. No frameworks. No build step. No npm dependencies. No TypeScript.** The site deploys to GitHub Pages as-is; the only deploy-time transform is the cache-stamp sed in `publish.yml`.
- **Thin HTML pages, one JS controller each.** Every `*.html` is a shell (head, `#rail`, `#stage`, one `<script type="module" src="assets/js/pages/X.js">`). All rendering happens in the controller.
- **The engine directory is DOM-free and Node-safe.** `assets/js/engine/*` runs identically in the browser and in Node inside Actions. The pipeline must never disagree with what the browser shows. Never import DOM APIs there.
- **Layers, strictly:** `lib/` (primitives: fmt, text, stats) → `engine/` (game logic) → `data/sources.js` (loaders + backends) → `viz/` (SVG builders) → `shell.js` (chrome + shared fragments) → `pages/` (controllers). Lower layers never import higher ones.
- **Data is generated by pipeline scripts, committed to `data/`, served statically.** Browser code never writes to `data/`; the personal logbook lives in `localStorage` (`exiva:logbook`).
- **`BACKEND = 'browser'`** — private-first: pages read hunt evidence from the local logbook. GitHub Issues/Discussions remain available as *optional* sharing backends. Do not flip the default without owner approval.

### Directory map

```
*.html                     one shell per page (index, character, grounds,
                           creatures, charms, submit, tools, analytics, admin)
                           — grounds.html and creatures.html each show their
                           dossier inline (?g=slug / ?c=slug) instead of a
                           separate page
manifest.webmanifest       installable-app manifest (standalone display, dark theme)
assets/img/icon.svg        home-screen icon source; icon-180/512.png generated
                           from it via qlmanage (regenerate when the mark changes)
assets/css/base.css        tokens (light+dark), shell, buttons, forms, sort-menu
assets/css/pages.css       page components (tiles, pills, filter bar, tools, viz)
assets/js/lib/             fmt.js · text.js · stats.js
assets/js/engine/          analyser.js · codex.js · strategy.js · locator.js ·
                           ledger.js · rules.js · progression.js · planning.js
assets/js/data/sources.js  dataset loaders + submission backends + logbook
assets/js/viz/svg.js       hand-rolled SVG charts (bars, flow)
assets/js/shell.js         ICONS, NAV, mountShell, shared fragments (ring, pills, sortMenu)
assets/js/pages/           _boot.js + one controller per page
data/                      bestiary, codex-extra, grounds, charms, access,
                           shared-hunts, ledger, character, per-category
                           highscore histories, character-snapshot
pipeline/                  check-hunt, merge-hunts, enrich-codex, enrich-art,
                           enrich-access, track-character, smoke
.github/actions/           commit-generated-data (owner-authored data commits,
                           optional Pages dispatch)
.github/workflows/         publish, check-hunt, merge-hunts, track-character
```

## 3. Philosophy and design principles

1. **Invent nothing.** Derive what arithmetic allows; when a number cannot be known, show "—" or ask the user for it. Never fabricate formulas, mechanics, or data. Every game mechanic must trace to TibiaWiki, TibiaData, the Cyclopedia exports, or the user's own input. (Violations already caught and removed: a fabricated "Major charms need level 250+" claim; a damage formula deriving min/max from level+skill; a tracker branch that copied yesterday's skill value into today's entry.)
2. **Honest data lineage.** Generated data carries its source (`source` markers, `wikiUrl` links, "unverified" caveats). A failed measurement is `null`, never a carried-forward value. History entries must satisfy the XP formulas (smoke-tested).
3. **Verify, don't assume.** Every change is checked in the browser preview and by `node pipeline/smoke.mjs` before commit. Every deploy is watched to completion and spot-checked live.
4. **The character is the centre.** New features must answer one of the practical questions in §1. Community/public features are optional extras, never the default surface.
5. **Small, boring, consistent.** No abstractions until the third use. No new files when an existing layer fits. Match the file's existing idiom.

## 4. Coding standards

- ES modules, single quotes, semicolons, 2-space indent, trailing commas in multiline literals.
- JSDoc block comment at the top of every file saying what it is; inline comments only for constraints the code can't express (never narration of the next line).
- CSS: compact one-line declarations per rule (match the existing files); design tokens only — never hardcode a colour, radius, spacing, or font that has a token (`--surface`, `--ink`, `--blue`, `--s*`, `--r-*`, `--t-*`, `--e-*`).
- Naming: camelCase functions/variables, SCREAMING_SNAKE for module constants, kebab-case files, semantic CSS class names by meaning not colour (`pill-success`, not `pill-green`).
- Vocabulary (post-pivot, fully renamed — keep consistent): hunt evidence basis is `curated | blended | logged` (never "community"); the shared dataset is `data/shared-hunts.json`; pages say "save"/"log", not "publish"/"submit to the community".
- Node scripts (`pipeline/*.mjs`): plain Node 22, no dependencies, `node:` prefixed imports, graceful no-op exits for already-done work, retry with backoff for flaky upstreams (TibiaData dev instance 429s and 5xxs).
- OWNER RULE (non-negotiable): every commit — human, agent or workflow — is authored as `nesleykent` (`20923096+nesleykent@users.noreply.github.com` for workflows, the owner's own email locally). The contributor list shows the owner alone. Never invent a noreply address, never use another bot identity, and never add agent/bot `Co-Authored-By:` trailers.

## 5. UI rules (Instagram Design System)

The interface follows the reverse-engineered Instagram Design System (see README "Design system adherence" for the audit history). Non-negotiables:

- **Shell:** desktop sidebar → narrow icon rail → mobile bottom tab bar (≤700px). GitHub + appearance toggle live in a floating top-right cluster on mobile, never in the tab bar. **The tab bar holds exactly 5 destinations** (Home, Character, Planner, Tools, Log a hunt — the daily loop; Instagram keeps five too); the other pages are desktop-sidebar + home-workspace only, marked by the NAV entries' 4th field (`mobile`) and hidden via `.rail-desktop-only`. Never add a 6th mobile tab without owner approval; verify at 320px whenever NAV changes.
- **Colour:** near-monochrome; one signature blue `#0095F6` for primary actions; the brand gradient (`--grad-*`) is otherwise whole-surface/whole-word only (`grad-text`), never a small accent — **except** three deliberate gradient-as-data-colour exceptions (owner-approved 2026-07-20, matching the reference mockup), all in `assets/js/viz/svg.js` unless noted:
  1. The primary time-series line + area in `flow()`/`sparkline()` wears the hero gradient (rose→magenta→purple).
  2. `bars()`: whichever row holds the actual max value (found by value, not row order — a chronological list like weekday averages isn't pre-ranked) wears the gradient; every other row is a flat de-emphasis grey (`rgba(var(--ink-2), .35)`), never blue. Matches the mockup's ranked-bar treatment (creature damage sources, XP/h by ground, ground population) — the gradient marks the answer, not just the longest bar.
  3. `.badge-highlight` (`assets/css/pages.css`): a gradient-filled badge for the single top-ranked item in a list (Planner's "Top XP" ground) — never a status tone, and never applied to more than one item per list.
  Bars' non-leader rows and dots outside the flow gradient stay blue. Donut/`categorical()` segments (non-elemental groupings only — profit by ground, never real element data) use a fixed brand-hue set (`--brand-rose/-magenta/-purple/-orange/-yellow`, RGB-triple twins of the `--grad-*` stops) instead of blue or the element palette, also matching the reference mockup; a genuinely elemental grouping keeps the `--c-*` palette so a reader can still tell fire from ice — the mockup's uniform gradient on every element badge was rejected as a readability regression, not reproduced. Element colours (`--c-*`) are *data* colours: meters, badge tint washes — never component chrome, borders, or icon tints.
- **Components:** `.btn` tiers primary/secondary/tertiary/destructive, flat 6px radius, letter-spacing -0.01em, disabled opacity .3. Two chip species, never conflated: `.badge` is the source Badge verbatim (solid tone fill, white 11px/700 uppercase, 4px radius) for genuine STATUS indicators only — basis (Curated/Logged/Blended), rules-check (Faulted/Flagged/Clean), evidence provenance; tones success/info/error are source colours, warning is the brand orange stop (documented extension — the source has no caution tone). `.pill` is Exiva's own quiet data/label chip (soft wash, ink text, pill radius) for categorical metadata and element readouts — element colours tint pill washes only, never badge fills. There is NO bordered Tag component in use — the source's Tag is an interactive filter chip this app doesn't have. `.panel` cards at 6px radius, hover-only shadow; art tiles at 12px (editorial variant).
- **Sort controls** use `sortMenu()`/`bindSortMenu()` from shell.js (anchored menu with checkmark, Apple HIG pattern) — never a native `<select>` for sort. Filter fields use `.lbl` + `.filter-bar` with the mobile collapse (`.filter-toggle`/`.filter-more`).
- **Mobile filter bars must not be sticky** (iOS Safari sticky+backdrop-filter ghosting bug — documented in pages.css).
- Light-first with dark appearance via `data-appearance`; both must be checked for contrast when touching colours.

## 6. Implementation patterns

- New page = new `X.html` shell copied from an existing one + `assets/js/pages/X.js` controller booting via `boot('X.html', { codex, grounds, hunts, config, ledger })` with only its real data dependencies enabled + NAV entry in shell.js (check 320px tab bar) + README pages-table row.
- Data loaders live in `sources.js`'s `FILES` map — **all data fetches go through it** (the deploy stamp seds that map for cache busting; a fetch outside it silently serves stale data).
- Charts: `bars()`/`flow()` from `viz/svg.js` — extend those, don't add chart libraries.
- Formulas: `engine/progression.js` (XP/level, base value — ported from tibia-xp-history) and `engine/planning.js` (stamina, profit, effective damage multipliers). New game math goes in engine with a source link and a smoke assertion.
- Character tracking: `pipeline/track-character.mjs` crawls TibiaData dev-instance highscores (the only public exact-XP source), same-day refreshes are allowed (highscores roll during the day), a page-1 snapshot guards staleness, and reruns no-op cleanly. Never write the snapshot before the run fully succeeds.
- Online tracking removed (2026-07-16): a second 15-minute-cadence workflow (`track-online.yml`/`track-online.mjs`/`data/character-online.json`) sampled world-list presence, but GitHub Actions' `schedule` trigger cannot reliably hold a 15-minute cadence at scale, so the sampler drifted and delivered unreliable data. Do not reintroduce online/presence sampling without owner approval.
- Cache busting: `publish.yml` stamps `?v=<sha>` into HTML asset tags, JS import specifiers, and the `FILES` data map at deploy time. Source files stay unstamped locally — never hand-write a `?v=` in the repo.

## 7. Forbidden changes

- No frameworks, bundlers, package.json, node_modules, or TypeScript.
- No invented game mechanics or formulas (see §3.1). No placeholder/fake data presented as real.
- No renaming datasets, basis vocabulary, or public URLs without a full-codebase consistency sweep AND owner approval (the community→logged sweep is done; don't churn it again).
- No touching `data/bestiary.json`, `data/grounds.json`, `data/charms.json` by hand — they are exports/curated inputs.
- No always-on card shadows, no colour-named CSS classes, no native sort `<select>`, no emoji in UI chrome, no new colour accents outside the token set.
- No sticky+backdrop-filter, at any width — `.filter-bar` is static everywhere (see §9's occlusion entry). No nav additions without the 320px check.
- Do not commit directly to `main` without running `node pipeline/smoke.mjs` and `node --check` on every touched JS file.
- Do not change the tracker's schedule, its no-op semantics, or history-entry shape without migrating the existing 31+ days of data.

## 8. Decision log (current, binding)

| Decision | Why |
| --- | --- |
| Home = doorway; `character.html` = dashboard | Owner-approved IA: home routes to workspaces, character page carries the intelligence |
| `BACKEND = 'browser'` | Private-first: evidence = my logbook; GitHub backends optional for sharing |
| Basis vocabulary `curated/blended/logged` | Post-pivot naming; fully swept |
| Tracker: TibiaData dev instance, 03:00 UTC, rows keyed by Tibia server-save day, same-day refresh allowed | Only public exact-XP source; mirrors tibia-xp-history's crawl, but history dates follow the real Tibia day boundary: 10:00 Europe/Berlin (CET/CEST), not UTC midnight |
| `track-character.yml` moved from a 15-minute cron to hourly (2026-07-17, owner-directed) | The 15-minute schedule was never actually honored — run history showed real gaps of 1-3+ hours, the same GitHub Actions scheduler-reliability failure already documented for the removed online-presence tracker below. An hourly cron is what the scheduler can actually deliver; `track-character.mjs`'s same-day-refresh logic is cadence-agnostic and needed no changes |
| Time online: TibiaData world-list sampling every 15 minutes | GuildStats-style online time is created by polling the public world online list; each row is an observed sample, not continuous telemetry or backfilled history |
| Highscore fetch failure → `null` | Honest lineage; never carry forward |
| Highscore tracker covers all current TibiaData categories | The character dashboard's "Highscores" section mirrors TibiaData's category surface: achievements, fighting skills, boss/bounty/charm points, Drome, experience, fishing, Goshnar's Taint, loyalty, shielding and weekly tasks |
| Each highscore category owns its history file (2026-07-19, owner-directed) | The combined `data/character-history.json` obscured whether a category was genuinely observed and coupled unrelated histories. `data/highscores/<category>.json` now stores one server-save-day series per TibiaData category as `{value, rank, source}`; Experience also stores level and retains all GuildStats backfill. Confirmed unranked results are explicit nulls, while transient fetch failures create no row. Browser loaders merge these files only as a compatibility view for existing dashboards. |
| Damage calc takes the player's raw min/max as input | Deriving rolls from level+skill needs weapon/wheel state the app can't know |
| Charm mechanics: per-stage value IS the trigger chance (5/10/11%), damage fixed at 5% of initial HP | Owner-corrected 2026-07-05 — an earlier flat "10% proc" constant was wrong; both numbers now come from each charm's own catalogue entry (stages + effect text), never a shared constant. Carnage (on-kill) and Minor utility charms don't fit the per-hit pattern and are excluded from advice |
| Stamina regen: 10-min offline delay, then 3 min/min up to 39:00, 6 min/min ("twice as long") for 39:00–42:00 | Owner-corrected 2026-07-05 — pinned by TibiaWiki's own worked example (39:00→42:00 = 18h10m), asserted in smoke |
| Element colours never on component chrome | Design-system audit (Tag has only purple/neutral accents; Badge has no dot) |
| Mobile tab bar = 5 destinations, rest desktop-only | Owner request 2026-07-05 ("keep only 5"); IG's own bar holds five |
| Status chips = source-exact `.badge`; labels stay quiet `.pill` | Closes the "colours don't match" 2do: the source Badge is a solid tone with white uppercase text, not a wash — but solid uppercase on every vocation/loot label would out-shout content, so only true statuses get it |
| Wheel of Destiny multipliers NOT added to the damage calculator | Research (docs/wheel-of-destiny-research.md): only 2 of ~30 perk effects are numerically unambiguous in the source, her actual Wheel build is unknowable from public data, and the player-entered damage range from the client already includes Wheel effects — stacking multipliers would double-count |
| Charm affordability = upper bound vs EARNED points | The charm-points highscore cannot see in-game spending; every affordability mark carries that caveat |
| Online sampler: 14-day raw window, older days compact to per-UTC-day summaries; observed level-ups persist before pruning | Bounds character-online.json (~96 samples/day forever otherwise) while `observed` counts keep absence-of-observation distinct from offline |
| Online sampler redeploys only on online/session-close/level-up/new-UTC-day | A Pages build every 15 minutes is waste; offline heartbeats commit without deploying (dashboard ≤1 day stale during offline stretches) |
| GuildStats backfill via Safari webarchives | Plain saves miss AJAX content; webarchives capture the rendered DOM |
| Generated-data workflows use `.github/actions/commit-generated-data`; Pages deploys queue instead of cancel | Keeps owner-authored data commits, optional redeploy dispatch, and no-op handling consistent across workflows; rapid pushes should not create red canceled deploy runs |
| Party loot-split tool rejected | Explicitly out of scope per §1's Respawn Finder note (no item timers, party split, blessing tools) |
| Imbuement cost calculator rejected (2026-07-06), then superseded same day | Original rejection: the per-imbuement ingredient/quantity table wasn't in this repo's data and no pipeline script fetched it. Superseded once the owner explicitly requested it with a manual-price-input model (no pipeline dependency) and item quantities researched from web guides live in-session. See "Imbuement calculator built" below for what's actually verified vs not |
| Imbuement calculator: TibiaMarket API prefill implemented (2026-07-07) | `pipeline/fetch-imbuement-prices.mjs` fetches Gentebra prices for Gold Token + all 70 imbuement items from api.tibiamarket.top (same endpoint/token as github.com/nesleykent/tibia-warzones-schedule's fetch_item_history.py; item→numeric-id pins in `pipeline/imbuement-market-ids.mjs`) into `data/imbuement-prices.json`, run by `.github/workflows/fetch-imbuement-prices.yml`. `assets/js/data/imbuement-prices.js`'s `mergeMarketPrices` layers per-world manual `localStorage` entries over the market prefill — manual always wins; an untouched field shows the market estimate (visually marked) and is never written to `localStorage` until edited. Only Gentebra is fetched; other worlds stay manual-only (fetching TibiaMarket for every Tibia world isn't feasible under its rate limit) |
| Sparse TibiaMarket imbuement prices use newest 30-day sell evidence (2026-07-16) | A two-day/latest-row-only fetch left all five elemental damage imbuements unpriced when one low-volume ingredient had no sell that day, and the code claimed to read active offers without inspecting `sell_offer`. The fetcher now scans newest-to-oldest across 30 days, preferring an active sell offer, then daily lowest sell, then daily average within the newest usable row. Generated entries preserve the row's `observedAt` and `basis` separately from fetch `updatedAt`; no observation still stays missing. Smoke tests pin the fallback order and timestamp lineage |
| Logbook JSON imports pass the same rules as live saves (2026-07-16) | The old importer accepted any object carrying a new `id`, allowing malformed rows and duplicate analyser text to poison planner/analytics consumers. `assessImport()` in the DOM-free rules engine now classifies valid rows, duplicate IDs/analyser payloads and invalid rows before storage; the admin UI reports each count and the first ten rejection reasons. `logbook()` also fails closed to `[]` when localStorage contains valid JSON of the wrong shape, while `writeLogbook()` refuses non-arrays. Import is a real keyboard-operable button (not a styled label), and all destructive/transfer controls meet the 44pt target on mobile |
| Logbook delete and duplicate sweep retain one-step undo (2026-07-16) | The browser logbook is the only copy unless the player exports it, so immediate irreversible deletion was disproportionate. Admin now keeps the snapshot immediately before a delete/sweep in memory and exposes a persistent Undo action beside the table; another destructive action replaces it, navigation clears it, and a successful import explicitly invalidates it so restoring old state can never erase newly imported hunts. No second persistence key or hidden history was added |
| Mobile interaction targets share a 44pt contract (2026-07-16) | The five daily-loop pages rendered without page overflow at the 375×667 iPhone 8 baseline, but shared navigation, utility controls, fields and regular/small buttons measured only 32–43pt. `--tap` now centralizes the 44pt mobile minimum across the shell and shared primitives; desktop density is unchanged, and the character hero reserves the full width of the enlarged floating utility cluster |
| Imbuement item quantities: all 23 are `verified: true` (2026-07-06, corrected) | The initial pass sourced ~18 imbuements from a single third-party guide (TibiaVault) and got several outright wrong — Dragon Hide, Quara Scale, Cloud Fabric, Demon Presence, Precision, Epiphany, Chop and Reap all had wrong items, and Vibrancy's third item was fabricated ("Swamp Plant" doesn't exist; the real item is Quill). Re-derived every tier's exact `astralsources`/`effect` straight from each imbuement's own TibiaWiki `{{Infobox Imbuement}}` template (`action=parse&prop=wikitext` on "Basic/Intricate/Powerful <Name>") — the wiki's own primary data, not a guide's transcription. If a number is ever suspect again, re-fetch the template, don't guess |
| Imbuement icons: `pipeline/enrich-imbuement-art.mjs` → `data/imbuement-art.json` | Mirrors `enrich-art.mjs`'s creature-art approach: TibiaWiki-hosted images only (static.tibia.com hotlink-blocks), resolved via the MediaWiki API and validated with a HEAD-check pass. Incremental/idempotent; re-run whenever the catalogue changes. All 93 icons (23 imbuement + 70 item, incl. Gold Token) resolved on the first real run |
| Imbuement totals clone Intibia's base + 100% success-fee model (2026-07-07, corrected from the failed current-fee pass) | The owner supplied live Intibia screenshots proving the expected Void/Gentebra totals: Basic 107,548, Intricate 231,598, Powerful 435,268 at Gold Token 46,274 / Rope Belt 4,296 / Silencer Claws 3,362 / Some Grimeleech Wings 1,734. Those only resolve with the reference calculator's base + 100% success fees: Basic 15,000gp, Intricate 55,000gp, Powerful 250,000gp. Keep `calculateTier().cheapest.total` fee-inclusive and smoke-test against those screenshot numbers |
| Gold Token cards show Intibia's normal item list plus per-row acquisition method | For Vampirism/Void/Strike, Intibia still lists the regular ingredient rows while the Total can use Gold Tokens or a hybrid package when cheaper. The Exiva modal mirrors that: rows remain canonical imbuement ingredients, rows covered by the selected token package show the Gold Token icon, remaining rows show the market icon, and the Total info popover compares Gold Tokens only vs Market only with the delta from the chosen cheapest method |
| Imbuement grid stays intentionally simple | The reference page opens with World + Tier controls, then one imbuement card per entry. Filters, sort menus, and the old overview Gold Token popover were removed because they made the tool feel unlike Intibia and added decisions before the user sees the prices |
| Shared Experience range and exercise-weapon calculators removed (2026-07-06) | Owner never asked for either; built speculatively in an earlier cycle and cut on request. Don't rebuild without an explicit ask |
| Character planner shortcuts never fall back to another vocation | The character page's "What next" section must not recommend Sorcerer/Knight/Paladin rows to Night'Flyn just because they have raw XP values. If no raw-XP-rated Elder Druid row exists in the current band, show an honest empty state and link to the full planner |
| Character dashboard uses overview + task tabs (2026-07-16, owner-directed) | Stable identity and actions form a slim header; Current level, Target level, Projected date, pace and one event-marked chart stay directly visible. Next hunt, Highscores, Hunt log and Details are real keyboard-operable tabs, replacing the long section-jump page and its repeated recent-days/level-breakthrough tables. Tabs group distinct workflows; they do not conceal duplicate copies of the overview. Highscores shows Experience once as the featured trend and every other category once in a compact list; Loyalty title lives in Details |
| In-page/hash navigation removed (2026-07-16, owner-directed) | No page uses fragment hrefs, URL hash APIs or hash-driven scroll behavior. Character deep dives use ARIA tabs without URL mutation; Charm advice uses query-backed route state; the global skip control is a button that focuses and scrolls the main landmark without changing browser history |
| Page bootstrap datasets are opt-in (2026-07-16) | `_boot.js` used to fetch the 3.2 MB creature codex, planner rows, hunt evidence and config on every controller—even Logbook, which needs none. Controllers now declare `codex`, `grounds`, `hunts`, `config` and/or `ledger`; `ledger` implies grounds + hunts. Keep dependencies explicit so unrelated pages do not regress into multi-megabyte startup work |
| Solo hunt evidence requires a vocation; team evidence does not (2026-07-16) | Vocation is decision-critical for solo recommendations, but a team hunt has no single correct vocation. The rules engine and native Log-a-hunt form enforce that distinction identically; do not make team logs invent one vocation merely to satisfy a generic required field |
| Charm recommendations use query-backed selection (2026-07-16) | Fragment-style charm links survived the original hash-removal sweep inside generated strategy HTML and became broken. Creature/ground advice now routes to `charms.html?charm=freeze`; Charms presents that card first and removes it from the catalogue below, preserving context without fragment navigation or duplicate content. Smoke scans every JS source for hash navigation |
| Home stays a data-light doorway (2026-07-16) | Home used to repeat level/tracker metrics from Character and download the 3.2 MB codex, 192 KB planner catalogue and 104 KB history only to count their rows. It now loads only `config.ini`, groups the four daily-loop destinations separately from reference/record workspaces, and exposes every desktop-only destination (Progress, Codex, Charms, Logbook) to mobile without expanding the five-tab bar. Each route appears once in the workspace map; duplicate Character/Planner hero actions were removed |
| Home adopts the standalone daily dashboard (2026-07-19, owner-directed; supersedes the doorway decisions above) | The owner explicitly rejected a route-directory translation as materially too far from the supplied standalone template. Home now uses real profile/history, ledger and private-logbook evidence for compact progression metrics, one next-hunt decision, honest attention items and shortcut chips. It still never loads the 3.2 MB codex or charm catalogue, and it never invents stamina, charm spending or analyser evidence. Character remains the deeper interactive profile surface rather than duplicating every dashboard section. |
| Planner and Codex selections preserve list context inline (2026-07-19, owner-directed) | The supplied standalone keeps filters and a compact result grid visible while showing the selected ground/creature dossier below. Route state remains query-backed, but selecting a card no longer replaces the entire working surface; closing the dossier restores the same filtered list without rebuilding user context. |
| Live filters never submit the page (2026-07-16) | Planner and Codex update results on every input, so native form submission only reloaded the route and erased keyboard-entered filter state. Both filter bars remain semantic `role="search"` forms but cancel `submit`; every button now carries an explicit type, with a repository-wide smoke guard preventing accidental submit buttons |
| Hunt planner (`grounds.html`) defaults its Vocation filter to the character's own vocation (2026-07-07 bugfix) | It already opened pre-set to the character's tracked level, but the Vocation filter defaulted to "All" — so the very first thing a Druid saw included Sorcerer/Knight/Paladin/Monk rows, contradicting the "What next" rule above for the exact same reason. `grounds.js` now resolves `characterVocation` from `profile.vocation` (promoted titles like "Elder Druid" always contain the base name, matched case-insensitively) and pre-selects it in `#f-voc`; still user-clearable back to "All". The filter predicate was also fixed to never exclude party/no-vocation rows even when a vocation filter is active — they're "any vocation" by design, not "no vocation matches" |
| Tracked character moved to `config.ini`, not hardcoded (2026-07-07) | The character name/world/vocation were hardcoded as `NAME`/`WORLD`/`VOCATION` consts across `pipeline/track-character.mjs`, `pipeline/track-online.mjs` and `pipeline/fetch-imbuement-prices.mjs`, plus scattered "Night'Flyn"/"Gentebra" literals in browser page copy. `config.ini` at the repo root is now the single source of truth: `pipeline/config.mjs` (Node, hand-rolled INI parser — project ships zero dependencies by design) feeds the three pipeline scripts and `pipeline/smoke.mjs`'s integrity assertions; `assets/js/lib/config.js` (fetches config.ini as text, same parser logic) feeds `_boot.js`, which every page destructures `config` from. Retargeting the whole site (trackers + UI) to a different character needs only editing config.ini, per "Deploying your own" in README.md. README's descriptive prose ("Night'Flyn's character dashboard", etc.) and static per-page `<meta description>` tags intentionally still name Night'Flyn — they document this specific deployed instance and can't be templated without a build step, which the project explicitly doesn't have |
| `config.ini` only configures `name` — world and vocation resolve automatically (2026-07-07, same day) | World/vocation were originally also hand-typed into config.ini, but TibiaData's character endpoint already reports both for any name — hardcoding them there would just be a second, driftable copy. `pipeline/config.mjs`'s `CHARACTER` is now resolved: prefer the already-tracked `data/character.json` (name match) with zero extra requests on the normal warm path (every scheduled run would otherwise re-hit TibiaData to reconfirm a fact that basically never changes); fall back to a live TibiaData character-endpoint fetch on cold start or a name mismatch (i.e. the owner just repointed config.ini at someone else). The promoted vocation title TibiaData returns ("Elder Druid") is mapped to the lowercase plural slug highscores URLs need (`druids`) via substring match against the five base vocations. The browser's `assets/js/lib/config.js` never needed world/vocation in the first place — every page already prefers `profile.world`/`profile.vocation` from `data/character.json` over the config fallback, so its `loadConfig()` only reads `name` and returns a generic ("Tibia") placeholder world for the pre-first-pipeline-run edge case |
| Hunt planner element pill respects the selected vocation's actual arsenal (2026-07-07 bugfix) | Cards showed an auto "best attack element" pill computed purely from the ground's creature-resistance data, with zero regard for whether the selected vocation can actually deal that element — e.g. a Druid-filtered card recommending Holy, which Druids have no way to cast. `VOCATION_ELEMENTS` in `assets/js/engine/rules.js` pins each vocation's real offensive elements, confirmed against TibiaWiki (not guessed, per this log's own "don't guess" precedent): Sorcerer fire/energy/death, Druid ice/earth, Paladin physical/holy, Knight physical, Monk physical (Monk's exact elemental spread beyond physical isn't confirmed, so kept conservative). `grounds.js`'s card render now picks the population's weakest element *among the ones the selected vocation can use* (`bestAttackElement`), falling back to the raw population-wide weakest element with no vocation filter selected. The manual Element filter dropdown (`state.element`) intentionally still matches against the raw, vocation-unrestricted top element — a user explicitly picking "Holy" there is a deliberate choice (e.g. a Paladin bringing holy runes), not an automatic recommendation |
| Ground creature matchups use explicit TibiaWiki rosters (2026-07-19, owner-directed) | Bestiary habitat similarity cannot identify a specific spawn: broad labels such as Yalahar, Drefia, Banuta and Ingol pulled unrelated regional creatures into battle plans. `pipeline/enrich-ground-creatures.mjs` now rebuilds `data/ground-creatures.json` from TibiaWiki Hunting Places `CreatureList` templates, filters every name through the canonical Bestiary, separates numbered/directional grounds and article sections, and preserves the linked Wiki page plus match method. A saved analyser weights battle advice but never replaces that roster: the matchup table keeps every Wiki mob, with no logged share shown for mobs absent from loaded hunts. Without logs, Wiki mobs are equal-weight; only then may the runtime fall back to creatures directly named by the ground label. Broad habitat inference is forbidden, and unresolved grounds show an honest empty state. |
| SU2026 Bestiary is pipeline-generated from TibiaDraptor (2026-07-19, owner-directed) | `pipeline/update-bestiary.mjs` POSTs every page of the public Cyclopedia endpoint, rejects incomplete/duplicated responses and pins the reviewed SU2026 metadata (833 creatures, 28,734 Charm Points, 8,497 Echo Warden points, 21 new creatures) before replacing `data/bestiary.json`. The generated snapshot keeps endpoint/version/release/timestamp lineage and is never hand-edited. TibiaData enrichment remains optional and incremental; TibiaWiki artwork remains the renderable art source. |
| Creature task speed and route rates preserve source grain (2026-07-19, owner-directed) | `data/creature-tasks.json` combines the owner's five task-speed tiers with a curated import of `Kusnier's Tracker.xlsx`. Speed is a creature-level owner classification; each kills figure remains a location-specific hour/lap observation with worksheet-row lineage. Multiple routes are never averaged into a universal rate, and the Hard worksheet's approximately level-750 Monk context stays visible. Planner filters match a ground when its resolved population contains a creature in the exact selected speed tier; cards show the fastest tier present. Creature dossiers list every route observation and the variability caveat. |
| Online/presence sampling removed (2026-07-16) | `track-online.yml`/`track-online.mjs`/`data/character-online.json` and the character dashboard's Activity section are gone. GitHub Actions' `schedule` trigger cannot reliably hold a 15-minute cadence at scale (documented scheduler drift/skipped runs), so the sampler's "online every 15 minutes" premise was never actually honored in practice. Superseded rows above (the online sampler's own decisions, and the "Online-history rule" below) are kept for history but no longer apply — do not reintroduce presence/online-time sampling without owner approval and a cadence GitHub Actions can actually guarantee |

## 9. Completed work

Analyser parser (all client formats) · hunt locator (word-boundary matching, audited) · battle/strategy engine · ledger with trust tiers · creature codex (833 creatures, TibiaData enrichment, TibiaWiki art) · charm catalogue (Cyclopedia export) · ground access notes (TibiaWiki, 259/410) · grounds planner with mobile-collapsed filters + HIG sort menu · daily character tracker (XP + all current TibiaData highscore categories + deaths) with 384 tracked/backfilled history rows (2024-10-22→) keyed by Tibia's 10:00 Europe/Berlin server-save day · generated-data workflow action + data contract (`data/README.md`) · character dashboard (slim identity → at-a-glance progression → event-marked XP chart → task tabs; grouped profile facts; selected-row inspector; vocation-safe planner shortcut) · tools page (stamina, element damage, imbuement prices, profit) · progression analytics · private logbook + admin tools · design-system adherence audits · deploy cache-stamping · smoke-test suite gating every deploy.

Cycle 5 hygiene/consistency debt: fixed the stale `GUIDELINES.md` link in README.md (→ `AGENTS.md`); fixed the stale "ten thin pages" count in README.md (→ eleven); removed banned publish/submit vocabulary from `sources.js`'s backend blurbs and submit.js-rendered messages (github-issues/github-discussions/static backend paths, currently dormant since `BACKEND='browser'`); reconciled the Node version claim so all five workflows + README agree on Node 22; extended `pipeline/smoke.mjs` to assert `isAnalyser()`, `armorSpots()`, `groundDossier()`, `nameCreatures()`/`population()`, and `progressWithinLevel`/`nextMilestoneLevel`/`baseValue`/`nextBaseBreakpointLevel`.

GitHub Actions runtime maintenance (2026-07-16): every first-party action pin now uses its current Node 24-compatible major (`checkout@v7`, `setup-node@v7`, `github-script@v9`, `configure-pages@v6`, `upload-pages-artifact@v5`, `deploy-pages@v5`). This removes the runner's forced Node 24 compatibility shim while preserving the project's explicit Node 22 runtime for pipeline scripts.

Cycle 5 hunt planner search depth: `grounds.js` now does multi-term AND search across ground, area, creature names, creature family, difficulty/rarity, and access quest/note text; adds Area, Element, and Creature type filters from existing access/codex/strategy data.

Online-history rule (superseded 2026-07-16 — see decision log): sampled time online used to come only from `character-online.json`, grown by a 15-minute TibiaData world-list workflow. That sampler was removed; do not reintroduce it without owner approval, and do not use GuildStats as a live source if it ever is.

Cycle 5 dashboard analytics: character dashboard now shows XP-gain streaks, rank-over-time charting, level-up cadence, and skill-window XP deltas; analytics page adds average XP gain by weekday with visible sample counts. (The gated sampled-online XP/hour, hour-of-day activity, session-block, and world-population widgets were removed with the online sampler on 2026-07-16.)

Cycle 5 tools additions (shared Experience level-range calculator, exercise-weapon mana/time helper) were added speculatively and later removed on 2026-07-06 — see §8. Cycle 5 is now closed — see the entries above and in §8 for its full scope, including the party loot-split rejection (imbuement calculator was rejected then superseded — see below).

Imbuement calculator built (2026-07-06; screenshot parity correction 2026-07-07): `assets/js/engine/imbuements.js` (pure catalogue + `getAcquisitionOptions`/`selectCheapestOption`/`calculateTier`/`calculateImbuement`, DOM-free, smoke-tested against the owner's Intibia Void/Gentebra screenshot totals and Yana's Gold Token packages) + `assets/js/data/imbuement-prices.js` (manual per-world prices in `localStorage`) + a new "Imbuement price calculator" section in `tools.html`/`tools.js` (Intibia-style World + Tier controls, compact overview cards, and a `<dialog>` detail modal with Basic/Intricate/Powerful tier cards). Totals are fee-inclusive using Intibia's base + 100% success-fee model; Market-only, Gold-Token-only (Vampirism/Void/Strike only) and non-double-counting hybrid splits remain available to the engine, but the UI surfaces the single cheapest total, per-row acquisition icons, and a Total info popover comparing Gold Tokens only vs Market only. A zero price only counts if explicitly marked `confirmedZero`; an all-missing tier never recommends anything. World is read from the tracked character (`character.json`'s `world`) but stays editable. Item/imbuement icons come from `data/imbuement-art.json` (TibiaWiki, via `enrich-imbuement-art.mjs`) — see the decision-log entries above for both the data-correction pass and the art pipeline.

Character narrative pass (2026-07-07; task-tab correction 2026-07-16): `character.html` now reads as Identity → Progression at a glance → one event-marked chart → Next hunt / Highscores / Hunt log / Details tabs. The exact same level/history meaning is no longer repeated in a recent-30 table and a level-breakthrough list; level-ups and overlapping recorded deaths are markers on the interactive chart, while the full row history remains on Analytics. Empty planner/log states are direct actions, every non-featured highscore remains in one compact list, and off-vocation planner rows are never substituted when no raw-XP-rated Elder Druid target exists.

Log-a-hunt accessibility pass (2026-07-16): the analyser paste field is programmatically labelled and described, parser feedback is an announced status region, and hunt-location candidates are native toggle buttons with `aria-pressed` state instead of clickable divs with hand-written keyboard emulation.

Page-bootstrap performance pass (2026-07-16): controllers now opt into only the datasets they consume. Logbook no longer downloads/parses the 3.2 MB creature codex or planner rows; Character and Progress skip the codex; Creature list skips planner/config/hunt data; Tools skips planner rows. Ledger mode remains one flag and automatically supplies grounds plus hunt evidence.

Tools accessibility pass (2026-07-16): stamina and damage results are polite atomic status regions, world/tier filter changes announce their summary, and the imbuement dialog is labelled by its imbuement name and described by its effect.

Log-a-hunt form/reliability pass (2026-07-16): parsed results receive focus, confirmation is a native form with browser validation and Enter submission, solo/team vocation requirements match the rules engine, verdicts are announced, and an `aria-busy`/disabled in-flight guard prevents rapid duplicate saves.

Hash-navigation completion pass (2026-07-16): the two remaining generated fragment links in creature/ground strategy advice were replaced with query-backed selected-charm routing. The selected card appears once at the top of Charms, documentation no longer advertises fragments, and smoke now scans every page shell plus every JS source directory for fragment links or hash APIs.

Home doorway pass (2026-07-16): removed the duplicate current-level/history/directory counters from Home, split destinations into the daily loop and reference/records, added the previously missing Charms and Logbook routes for mobile users, and reduced Home startup from config + character/history + the complete codex + planner catalogue to `config.ini` alone.

Home hierarchy follow-up (2026-07-16): removed the Character and Planner hero buttons that immediately repeated the first two workspace cards. The hero now explains the product outcome, while the workspace map owns navigation with one route per player destination.

Live-filter keyboard pass (2026-07-16): Planner and Codex search forms now prevent native submission, so pressing Enter preserves the live result set, query and URL instead of reloading the page. The two remaining implicit buttons gained `type="button"`, and smoke rejects any future button without an explicit type.

Highscores consolidation pass (2026-07-17): removed the six-card top-rank strip and achievement summary that repeated values immediately below. The Highscores tab now has one Experience trend and one complete compact list of every other tracked category; the unique Loyalty title moved into Details, and the unused summary code/CSS was deleted.

Design-system sync pass (2026-07-19): synced against the companion Claude Design project's updated `templates/exiva-xp/*.dc.html` page compositions. Grounds and Codex merged their list/detail page pairs into one page each — `grounds.html` (`ground.html` deleted) and `creatures.html` (`creature.html` deleted) now show the selected ground/creature dossier inline via `?g=slug`/`?c=slug`, with a back control, `history.pushState`/`popstate` deep-linking, and the filter bar hidden (not lost) while a dossier is open; every internal link updated accordingly. Tools gained back a "Level target" (days-to-goal) calculator and level/experience table, deliberately rebuilding the feature removed 2026-07-06 per fresh owner approval — pace is derived from the character's own gap-aware daily XP history (`experienceForLevel`/`experienceUntilNextLevel` from `engine/progression.js`), never fabricated, and shows no projection when pace data is missing. Submit hunt gained a numbered/lined step-progress indicator (Paste → Read → Location → Confirm) that shows one step at a time with working back/forward navigation, collapsing to icon-only circles under 520px; all existing parsing/validation/accessibility behavior (labelled fields, announced status regions, `aria-pressed` location candidates, focus-on-parse, in-flight save guard) is preserved. Analytics gained a "Profit share by ground" donut (hand-rolled inline SVG, same convention as `viz/svg.js`'s other charts; a ground only appears with real logged profit, never zero-filled) and a "This week vs last week" comparison band reusing `character.js`'s `pace-band` pattern — the comparison honestly falls back to a "not enough data yet" note when tracked history/logged hunts don't reach back into the prior week, rather than showing a misleading 0-vs-0. Explicitly rejected per owner direction and left unbuilt: Home KPI/hero dashboard (conflicts with the data-light-doorway decision above), a Logbook status/review column, and a Character "Share profile" link or "Online" badge (no such data model exists for any of the three).

Imbuement price-input bug pass (2026-07-19): fixed a real regression from the price-editing UX built 2026-07-06/07 — editing a TibiaMarket-prefilled price only re-rendered the tier cards, never the price-input row itself, so the blue "estimate" highlight never cleared and the reset-to-estimate icon never appeared after an edit (the icon only ever showed correctly after a full modal re-open, e.g. via reset-then-reopen). `openImbuementModal` now has a `refreshPriceRow(itemId)` helper that updates just that row's highlight class and reset button in place (never regenerating `#imb-price-inputs` wholesale, which would drop focus mid-keystroke); reset itself now also updates the input's own value/highlight without a jarring full modal rebuild. Also fixed the per-item copy button copying `"25x Vampire Teeth"` instead of the plain item name a market search needs — it now copies `it.name.trim()` only (the tier-total "copy shopping list" button is unaffected; a full shopping list legitimately needs quantities). Removed the diamond/rotated-square "Gold pieces" `COIN_ICON` from all three gp-total displays — it wasn't in `PageTools.dc.html`'s own design intent (plain `"X gp"` text, no icon) and read as an unlabelled, ambiguous glyph in practice.

KPI + avatar correction (2026-07-19, owner-directed): the Deaths KPI card was replaced with a Charm points card (tracked highscore value with its since-first-tracked delta; deaths stay in the Recent deaths strip and the Details table). The avatar treatment was briefly changed to a quiet neutral disc, then superseded by the owner's correction below.

Hunt-link ring correction (2026-07-19, owner-directed): restore the conic-gradient `.ring` treatment for ground and creature initials, while removing the character dashboard's social-media-style Next hunts/stories block entirely. The ring remains available for actual ground and creature surfaces; there is no stories-like rail on Character.

Character standalone-layout pass (2026-07-19, second pass): the dashboard's four metric cards now follow the template's KPI shape — Total experience with an inline total-XP sparkline and a month-to-date delta (cumulative XP deltas stay honest across tracker gaps), XP/day pace with the existing 7-vs-30-day insight as a coloured delta, Profit/hour from the logbook (dash + "No logged hunts yet" when empty), and Deaths over the last 30 days with a prior-30-day delta. The XP chart's 7d/30d/All ranges became the template's Year + Month segmented tabs — years come from the tracked history, and the month row only offers months that actually have rows (2026 correctly omits its Mar–Apr tracker gap). Added the template's two-column "Daily XP activity" heatmap (26 tracked weeks, quartile intensity over real positive gains; an unmeasured day renders as a distinct dashed cell, never zero) beside a "Recent deaths" top-3 list (the complete table stays in Details). Skills meters and the points-card strip from the template were deliberately not imported — they would need invented %-to-next values and would reverse the 2026-07-17 highscores consolidation.

Level target inputs pass (2026-07-25, owner-directed): the Tools "Level target" calculator now states the goal as an explicit Starting level + Target level pair — the starting level was promoted out of the "Level & experience table" disclosure, where it sat beside the table's own From/To controls and read as a table setting rather than the projection's own input — plus two optional planning fields, Exp per hour and Hours per day. A planned pace only replaces the tracked daily average when both optional halves are filled, since an exp/hour with no hours/day says nothing about how fast the days pass (and vice versa); exp/hour on its own still produces the new "hunting time needed" KPI, so a goal can be priced in hunt hours without committing to a daily schedule. The result strip names the pace it actually used ("Planned pace 15kk/day · 5kk/h × 3h" vs "Tracked pace 18.42kk/day") alongside a levels-to-go pill, and keeps the honest no-projection fallback when no pace exists at all. The real-XP anchor is unchanged: the character's tracked experience backs the estimate only while the starting level still matches the tracked level, otherwise the level formula backs an explicit what-if. Verified at 375px (iPhone 8 baseline: all five fields stack full-width, no horizontal scroll) and at 1280px (3+2 wrap inside the tool card).

Daily XP capture-time correction (2026-07-25, owner-reported): Home's Today's XP read "+17.96M as of 2026-07-25 at 07:01 BRT" for a gain that was actually observed at 10:55 BRT. `track-character.mjs` stamped `capturedAt` once per Tibia day and reused it for every later run of that day, so the timestamp named the day's *first* reading while the value came from the newest one. Because a Tibia day's first reading is usually still yesterday's total (07:01 BRT recorded 1,980,965,670 — byte-identical to 2026-07-24), the run that finally recorded real progress silently inherited a pre-dawn stamp, and the hub credited a full day of hunting to a moment hours before it happened. `capturedAt` now refreshes whenever the reading itself changes and is kept only when the reading is identical; the change-detection comparison excludes `capturedAt`, so the timestamp can never be its own reason to rewrite a row. That keeps §7's no-op semantics intact — an unchanged hourly run still writes nothing and commits nothing (verified across five cases: first reading, identical rerun, value move, rank-only move, and a legacy row with no `capturedAt`, which stays absent rather than being back-stamped with a time it was never read at). The already-stored 2026-07-25 row was corrected to `13:55:01.667Z`, the moment the tracker's own run log printed `experience: rank 300, level 495, xp 1998928605` (Actions run 30160622210) — not the run's commit time, which trails the read by four minutes. No history-entry shape change, so no data migration. `home.js`'s formatting was already correct (America/Sao_Paulo, `h23`; BRT has no DST) and was left alone.

Filter-bar occlusion fix (2026-07-25, owner-reported): on Planner and Codex the sticky `.filter-bar` was covering the results it filters. Both pages pack eight or nine fields into it, so it measured 362px on Planner and 346px on Codex — 43–45% of an 800px viewport — and `position: sticky; top: var(--s2)` parked all of it over the grid while scrolling, clipping the top half of every row that passed underneath (ground and creature names included; measured 3 overlapped cards mid-scroll on each page). `.filter-bar` is now static at every width, which also retires the mobile-only sticky/backdrop reset added earlier for the iOS ghost-copy bug — the same failure mode from the other direction, so one rule now covers both. §7 updated accordingly. The bar keeps its own surface and its `.sort-menu` popups still paint above the card grid: the popups carry an explicit `z-index: 20` while the cards are positioned at auto, so dropping the bar's `z-index: 10` stacking context changed nothing (verified by hit-testing the open Sort popup over the grid, top and bottom edge). `#imb-filter-bar`'s dead `position`/`z-index`/`backdrop-filter` resets went with it. `table.grid th`'s sticky header is untouched — it sticks inside its own scroll container, not over the page.

Druid raw-XP stand-ins (2026-07-25, owner-directed): tibiapal rebuilt its hunting tables per vocation and has filled the Druid "Raw exp" column for only 33 of 162 rows, so most druid grounds in the planner had no XP/h at all and could never rank. Its retired combined Mage table at `/hunting-old` still carries figures for 89 places, and the owner directed using those until the druid column is finished. `pipeline/fetch-druid-xp.mjs` fetches both tables and writes `data/grounds-xp-legacy.json` — 54 stand-ins matched from 128 gaps. These are mage numbers, not druid measurements, so the whole path is built to keep that visible: `grounds.json` is never touched (it stays the pristine capture, so a re-capture never has to be un-merged), `normalizeGrounds` layers the overlay *underneath* it so a published druid value always wins, every filled row carries `xpRawFrom`, and `standInPill()` renders an amber "Mage stand-in" chip anywhere such a number appears (planner cards, dossier Basis column, Home's Next hunt). A stand-in also self-retires the moment tibiapal publishes that row. Matching is deliberately strict, since a wrong ground is worse than a missing number: exact names first, then cleaned token sets (parentheticals, technique words and plurals dropped, tibiapal's abbreviations expanded) accepted only on a 1:1 pairing. Compass directions and floor markers stay significant ("Werehyaenas South" never inherits North's 2.2kk; "Werecrocodiles -1" never inherits the unnumbered row), and a negated qualifier blocks a match ("Oramond West (No Quara Raid)" never inherits plain "Oramond West"). The 74 declined rows are reported by reason on every run — 70 have no legacy row at all, 3 would have shared one "Otherworld" row three ways, 1 is a negated variant. Smoke tests assert the overlay only fills blanks, stays within Druid, never restates a published value, leaves every other row byte-identical, and that no stand-in can exist without full provenance.

Stand-in profit + badge removal (2026-07-25, owner-directed, same day as the entry above): two owner corrections to the druid stand-in work. (1) The overlay now fills profit/h as well as raw XP/h, each field independently — a druid row can have a published raw exp and still be missing its money figure, so 67 rows carry a stand-in (54 raw exp, 67 profit). tibiapal's per-hour money column is a net figure (it goes negative on supply-heavy grounds — SD hunts read -200k), so it is a profit rate, not gross loot; with no logged hunts to beat it, `buildLedger` now uses it as a curated row's `profitRate` instead of dropping it. That fills profit/h for every vocation's curated rows, not just druid stand-ins. The Loot/h column still shows the same source number, since it is the one money column tibiapal publishes — worth revisiting if the duplication bothers anyone. (2) The "Curated" and "Mage stand-in" chips are gone per owner direction: `basisPill` returns nothing for the default `curated` state (Logged/Blended still render, and stand out more for it), and `standInPill` became `standInTitle` — the provenance now rides in the value's hover text and in `xpRawFrom`/`lootFrom` on the data, so nothing is lost from the record, only from the visual noise. This deliberately relaxes the §3.1 labelling stance for this dataset; the numbers remain traceable, just not badged.

Dossier-as-page fix (2026-07-25, owner-reported): opening a ground or creature left the list page stacked above it — `grounds.html?g=marapur-nagas` began with the "Hunt planner" header, the full filter bar and three cards, and the dossier's own "← Hunt planner" control sat ~950px down. Fine when opened by click (`openDetail` scrolls the dossier into view) but wrong for the shared-link and browser-back paths, which land at the top of the document. Both pages now hide `#planner-head`/`#codex-head`, the filter bar and the tile grid while a dossier is open, so it reads as its own page starting at the back control; the codex's `.show-more` wrapper goes too, since its top margin alone left a blank row. Hidden, never emptied — closing (or browser-back) puts the list straight back, filters intact, as verified for all three paths. This also fixed a latent trap: `[hidden]` had no effect on `.filter-bar` because the UA's `[hidden] { display: none }` loses to any class that sets a display, and `.filter-bar` is `display: flex`. base.css now carries a global `[hidden] { display: none !important }`, so the attribute means what it says everywhere.

Ground roster + area correction (2026-07-25, owner-reported "wrong creatures and wrong locations"): two independent defects, both now fixed at the source. (1) Rosters were polluted by the overworld. A TibiaWiki Hunt article usually carries several `{{CreatureList}}` blocks, and the first is often the fauna standing above the entrance — Vandura Rotworm Cave literally opens with `caption=Surface Creatures`, which is why the rotworm ground advertised Butterfly, Parrot, Flamingo, Seagull and Skunk. `creatureList()` now drops ambient-captioned blocks (surface/surroundings/outside/above ground/entrance) whenever the article also has a grounded list, unless the ground is itself the surface hunt ("Hive Surface", "Issavi Surface", "Krailos Surface"). 14 rosters shed ambient creatures; the rotworm cave went from 14 creatures to the right 6. (2) Areas were inferred from the wrong article. access.json resolves a Geography page and falls back to its first `near` link, so Oramond Catacombs read "Cormaya" (it is Rathleton) and Marapur Nagas "Roshamuul" (it is Marapur). `Infobox Hunt` states `city` outright, so the roster pipeline now records the resolved hunting place's own city and `grounds.js` prefers it, keeping access.json's area only as a fallback. All 330 resolved grounds now carry a city — access.json only ever had 259 areas, several of them wrong. Still open: 82 of 412 grounds resolve to no hunting place at all (listed by the pipeline on every run), and the ground→article pairing for many others still rests on the hand-curated `PAGE_RULES` map rather than checkable evidence — the next pass should score each pairing against the article's own creature list and level bracket, and refuse a match that nothing corroborates.

Ground pairing validator (2026-07-25, follows the roster/area correction above): every ground→article pairing is now scored against evidence that can be checked, and one that nothing corroborates is refused rather than shown. A pairing needs a *strong* signal — the label names a creature the article actually lists, or spells out the article's own title — or else three-way corroboration: CipSoft's `locations` string in `bestiary.json` independently places three or more of the article's creatures there AND the ground's city or level bracket agrees with the article's. City and level alone can never establish a pairing, since every ground in a region shares a city and many share a level band. Creature matching is genus-level, because Tibia names creatures genus-first and labels name the genus, not the rank: requiring every word refused "Nagas"→Temple of the Moon Goddess and "Coryms Port Hope"→Tiquanda Corym Cave, which are right. Title matching ignores grammatical filler, so "The Hive" is the hive and "Grounds of Plague" is the plague seal. Result: 289 of 410 grounds resolved (was 330), each carrying the evidence chain that kept it; 41 previously-shown pairings were refused and now display no creature list at all. That is the intended trade — a wrong roster reads exactly like a right one, so no roster is better. The refused list prints on every run and needs owner knowledge to settle: the Ferumbras seal rooms (labels use boss names — Pumin, Infernatil, Mazoran, DT — that appear nowhere in "Grounds of Deceit/Fire/Destruction"), Edron Heroes -2/-3, Edron Were South, the Nimmersatt floors, Oramond West, Oramond Wildlife Raid, Marapur Turtles, Temple of the Sun and Sea, Haunted House and Azzilon Walls. §9's smoke floor moved 325 → 280 to match, and now also asserts every surviving roster records its evidence and its city.

Spelled-out creature veto (2026-07-25, owner-reported "Crypt Warriors still wrong"): the ground is named after `Crypt Warrior`, which lives in Bounac (city Thais); it was paired with Kilmaresh Catacombs (city Issavi), whose roster holds the Crypt *Warden* — a different creature. Two causes, both fixed. The `PAGE_RULES` entry hand-written by an earlier session pointed at Kilmaresh Catacombs and was simply wrong; it now points at Bounac, which is added to `BROAD_PAGE_TITLES` so the roster narrows to the named prey instead of inheriting the whole area's chickens, bears and deer. More importantly the validator had no way to catch it: genus matching accepted the pairing on the shared word "crypt". `pairingEvidence` now applies a hard veto before scoring — when a label spells out a specific creature in full, the article must list that creature or the pairing is refused outright, whatever else it scores. Single-word names are exempt, so "Elves" is not forced to find a plain `Elf` among Elf Scouts and Elf Arcanists. The veto immediately caught three more of the same class that were live: Putrid Mummy→Horestis Tomb, Floating Savants MoTA→Museum of Tibian Arts and Haunted Treelings Vengoth→Vengoth, none of which list the creature the label names. 285 of 410 grounds now resolve.

Three vetoed grounds re-homed (2026-07-25, owner-reported "Putrid Mummy isn't Horestis Tomb"): the veto above was right to refuse all three, and each had a different cause. Putrid Mummy is in **Caverna Exanima** (Darashia) — both `bestiary.json`'s `locations` and the creature's own wiki page say so, and Horestis Tomb lists no Putrid Mummy at all. Floating Savants and MoTA Fury are in **The Extension Site** (Thais), not the museum proper, whose article holds animated exhibits and Goldhanded Cultists. Both `PAGE_RULES` entries were corrected. The third was a bug in the ambient filter itself, not a bad rule: Vengoth captions a genuine sub-area of the hunt "Vengoth, surface", and dropping it deleted Haunted Treeling — the one creature the ground is named after — which then tripped the veto. `creatureList()` now restores any list it filtered out that holds prey the label names, so the filter can never hide the ground's own quarry. The rotworm fix is unaffected: that label names `Rotworm`, which lives in the grounded list, so the surface list stays dropped and the butterflies stay gone. Coverage back to 289 of 410 with no regressions.

Ferumbras seal rooms uncrossed (2026-07-25): two more `PAGE_RULES` entries were provably wrong, caught by reading the articles' own rosters. Grounds of Destruction is the seal holding the **Juggernauts** (with Destroyer, Fury, Vexclaw, Hellflayer) — it was mapped to Grounds of Despair, which holds Spectres and Hands of Cursed Fate. The **Dark Torturers** are in Grounds of Damnation, which was mapped to Grounds of Destruction. Both corrected, and `PHRASES` now expands the player shorthand `dt` → "dark torturer" and `feru` → "ferumbras" so the evidence check can see the prey a label actually names rather than treating it as noise. Coverage 289 → 295 of 410, refusals 41 → 35, with the earlier corrections (Bounac, Caverna Exanima, Vandura Rotworm Cave, Temple of the Moon Goddess, Cobra Bastion) all intact. Method note for the next pass: `bestiary.json`'s `locations` is the fastest way to check a rule — it named Grounds of Damnation for Dark Torturer and Grounds of Destruction for Juggernaut before either article was opened.

Stamina recovery on the player's clock (2026-07-25, owner-directed): "18h10m offline" still leaves the player working out when they can log back in, so the stamina calculator now states it in words against the browser's own time — "Now it's 21:35. You need to rest 17 hours and 28 minutes to get from 39:07 to full stamina. If you start resting now, you will reach full stamina tomorrow, 26 July 2026 at 15:03." Everything is read from the browser, so it is the user's real local time and timezone with no server clock involved and nothing to configure; `#stamina-out` re-renders every 30s so "now" cannot go stale on a tab left open, and only that block is rewritten so a half-typed stamina field is never disturbed. Durations are spelled out, the arrival date carries today/tomorrow when it lands close, a target below 42:00 is named instead of called "full", and an already-rested case says so rather than projecting a date.

## 10. Active tasks (in priority order)

1. **Improve what already exists (owner-directed, 2026-07-17, standing priority).** Default mode of work is auditing and polishing the shipped surfaces — correctness bugs, UX rough edges, accessibility, consistency, performance, dead code — over adding new speculative features. A new feature needs an explicit owner ask or a §12 proposal the owner has actually approved; it does not get invented because it sounds plausible.
2. **Historical backfill** — extend `data/highscores/experience.json` when the owner supplies older GuildStats webarchives.
3. **Highscore-history accumulation** — keep collecting TibiaData rows; per-highscore cards intentionally wait for at least two distinct values before drawing a trend.

Done recently (2026-07-06): Highscores pass — renamed the character "Skills" section to "Highscores" and expanded the TibiaData tracker from the old seven-category subset to all current highscore categories, with shared descriptors in `assets/js/engine/highscores.js`. GitHub/data hygiene pass — generated-data workflows now share `.github/actions/commit-generated-data`, Pages deploys queue instead of canceling older in-flight runs, shared-hunt merges dispatch Pages when visible data changes, and `data/README.md` documents curated inputs vs generated caches vs character observations. Fixed TibiaData highscore-history dating to use Tibia's 10:00 Europe/Berlin server-save day; the pre-save 2026-07-06 UTC reading now refreshes the 2026-07-05 Tibia day instead of creating a wrong next-day row. Historical XP/rank backfill extended from owner-supplied GuildStats rows and Safari webarchives, covering 2024-10-22 through 2026-06-03 before the existing daily tracker window; blank source ranks are preserved as `null`. 2026-07-05, cycle 4: character UX pass — dashboard-first header, section jump bar, page-level timezone selector, XP chart inspector, mobile/tap chart selection, and per-highscore analytics cards with independent scales; `track-character.mjs` now stores rank fields beside each tracked highscore value when TibiaData returns them. Cycle 3: personal charm recommender (charmAdvice() in planning.js — expected proc damage over logged kills, smoke-tested against real codex data) and logged-kill counts on creature dossiers (labelled as a floor, never as Bestiary state). Earlier same day: badge-colour audit closed (source-exact `.badge` for statuses; token values verified 1:1 against the design project's `colors.css`); planner search matches area names + Level defaults to the tracked character level; analytics highscore-trend boards; PWA install support (manifest + icons, all pages); charms page shows the tracked charm-point budget with per-stage affordability (earned-points caveat); Wheel of Destiny researched and closed as a no-implement decision (see §8 and docs/wheel-of-destiny-research.md).

## 11. Handover workflow (mandatory for Codex)

When continuing implementation:

1. Read this file, then `README.md`, then the files you will touch. Do not start from the prompt alone.
2. Work ONLY on tasks from §10 or explicit owner instructions. Architectural decisions (new pages, renames, data shapes, dependencies, IA changes) are NOT yours — leave a proposal in §12 instead of implementing.
3. For every change: match the surrounding idiom, keep the layer rules (§2), keep the vocabulary (§4), keep the UI rules (§5).
4. Before every commit: `node pipeline/smoke.mjs` passes; `node --check` passes on every touched JS file; the affected pages render error-free in a local preview at 375px and desktop; anything touching NAV or the tab bar is also checked at 320px.
5. Commit messages follow the existing style (`fix:`/`feat:`/`data:` + why-focused body). Push, watch the `publish.yml` run to completion (`deploy-pages` fails transiently — retry via `gh workflow run publish.yml`, never `gh run rerun --failed`), and verify the live site.
6. Update §9/§10 of this file when a task completes or new work is discovered. Log notable decisions in §8 — with the reason.
7. If unsure whether something is allowed: it isn't. Stop and leave a note in §12.

## 12. Open questions / proposals for the architect

*(empty — add entries here instead of improvising)*
