; Exiva XP tracks one Tibia character. Change these to point the whole site
; (pipeline trackers + browser UI) at a different character — no code edits
; needed. See AGENTS.md's decision log for what reads this file.
[character]
name = Night'Flyn
world = Gentebra
; TibiaData's highscore URLs take a lowercase, unpromoted vocation slug:
; knights, paladins, sorcerers, druids, monks.
vocation = druids
