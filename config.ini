; Exiva XP tracks one Tibia character. Change `name` to point the whole
; site (pipeline trackers + browser UI) at a different character — no
; code edits needed. World and vocation are resolved automatically from
; TibiaData, not configured here. See AGENTS.md's decision log.
[character]
name = Night'Flyn
